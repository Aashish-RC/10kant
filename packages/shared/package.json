{
  "name": "@ra1/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript types for RA1",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "dev": "npx tsc --watch -p tsconfig.json",
    "start": "tsc -p tsconfig.json"
  },
  "config": {
    "build": "tsc -p tsconfig.json"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "private": true
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}